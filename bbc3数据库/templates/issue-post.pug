extends layout.pug 

block content 
  if isLogin
    h2 发帖
    form(action='/post' method='POST')
      div Title: 
      input(type='text' name='title')
      div Content: 
      textarea(name='content' rows='10' cols='30')
      div
      button 发帖
  else 
    p 未 #[a(href='/login') 登录]，不能发贴