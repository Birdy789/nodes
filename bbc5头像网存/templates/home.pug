extends layout.pug 


block content 
 ul 
 each post of pagePosts
   li 
    a(href ='/post/'+post.postId)=post.title
    |  #{ 'by' }  
    span= post.name
 p 
  if page > 1
    a( href=`/?page=${page - 1}`) 上一页
    |
    span=page
    | 
  if(page !== totalPage) 
    a( href=`/?page=${page +1}`) 下一页












  //   ${req.isLogin ?
  //       ` <a href="/post">发贴</a>
  //     <a href="/logout">登出</a>`:

  //       `<a href="/login">登陆</a>
  //     <a href="/register">注册</a>`
  //     } 
  //     </div>
  //   <ul>
  //    ${pagePosts.map(post => {//  ${posts.map(post => {
  //       return `
  //        <li>
  //        <a href ='/post/${post.id}'>${escape(post.title)} </a> <span> by ${post.postedBy ? post.postedBy : '未知用户'}</span>
  //        </li>
  //        `
  //     }).join('\n')
  //     }
  //   </ul>

  //   <p>
  //   <a href="/?page=${page - 1}">上一页</a>
  //   <a href="/?page=${page + 1}">下一页</a>
  // </p>
